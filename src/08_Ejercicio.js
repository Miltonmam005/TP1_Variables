let num1 = parseInt(prompt("Ingrese el primer número:")); Division = num1 % 2;

if (Division === 0) {
    document.writeln(num1 + " Es un número dividible en 2.")} 
    else {
    document.writeln(num1 + " No es un número dividible en 2.")};